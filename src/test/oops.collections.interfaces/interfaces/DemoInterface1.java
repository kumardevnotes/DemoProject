package com.oops.interfaces;

public interface DemoInterface1 {
	
	/* Interface is similar to class but it should start with the keyword interface
	 * Interface contains only method signatures or method declarations without method body
	 * It is the Class responsibility to define the method body of the implemented interface
	 * */
	
	
	
	public void displayMessage();
	public void displayMessage(String myMessage);
	public int findTheArea(int length, int width);
	
	
}
